# Example runtime config for the new workflow skeleton.
#
# This file can provide local defaults for model/runtime validation.
# Environment variables still override these values.

[runtime]
model = "gpt-5"
timeout_s = 90

[openai]
api_key = "your_api_key_here"
base_url = "https://api.openai.com/v1"
